apiVersion: v1
kind: Service
metadata:
  name: {{ include "kryptos.fullname" . }}
  labels:
    {{- include "kryptos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kryptos.selectorLabels" . | nindent 4 }}
