image:
  repository: maziyarpanahi/openmed
  tag: latest
  pullPolicy: IfNotPresent

service:
  main:
    ports:
      http:
        port: 8000

ingress:
  main:
    enabled: false

persistence:
  models:
    enabled: false
    mountPath: /data/huggingface

nodeSelector: {}
affinity: {}
tolerations: []
