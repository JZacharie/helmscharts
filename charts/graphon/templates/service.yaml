apiVersion: v1
kind: Service
metadata:
  name: {{ include "graphon.fullname" . }}
  labels:
    {{- include "graphon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.service.port | default 8080 }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "graphon.selectorLabels" . | nindent 4 }}
