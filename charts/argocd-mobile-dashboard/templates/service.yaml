apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-mobile-dashboard.fullname" . }}
  labels:
    {{- include "argocd-mobile-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-mobile-dashboard.selectorLabels" . | nindent 4 }}
