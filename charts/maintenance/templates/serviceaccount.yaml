{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "maintenance.fullname" .) }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "maintenance.labels" . | nindent 4 }}
{{- end }}
