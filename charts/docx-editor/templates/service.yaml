apiVersion: v1
kind: Service
metadata:
  name: {{ include "docx-editor.fullname" . }}
  labels:
    {{- include "docx-editor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "docx-editor.selectorLabels" . | nindent 4 }}
