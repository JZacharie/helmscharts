apiVersion: v1
kind: Service
metadata:
  name: {{ include "alexa-leclerc-mcp.fullname" . }}
  labels:
    {{- include "alexa-leclerc-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alexa-leclerc-mcp.selectorLabels" . | nindent 4 }}
