apiVersion: v1
kind: Secret
metadata:
  name: {{ include "shannon.fullname" . }}-secrets
  labels:
    {{- include "shannon.labels" . | nindent 4 }}
type: Opaque
stringData:
  anthropic-api-key: {{ .Values.shannon.anthropicApiKey | quote }}
  claude-code-oauth-token: {{ .Values.shannon.claudeCodeOauthToken | quote }}
