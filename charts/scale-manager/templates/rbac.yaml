apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "scale-manager.fullname" . }}
  namespace: {{ .Values.namespace }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "scale-manager.fullname" . }}
  namespace: {{ .Values.namespace }}
rules:
  - apiGroups: ["apps"]
    resources:
      - deployments
      - deployments/scale
      - statefulsets
      - statefulsets/scale
    verbs: ["get", "list", "watch", "patch"]

  - apiGroups: [""]
    resources:
      - configmaps
    verbs: ["get", "list", "create", "update", "patch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "scale-manager.fullname" . }}
  namespace: {{ .Values.namespace }}
subjects:
  - kind: ServiceAccount
    name: {{ include "scale-manager.fullname" . }}
roleRef:
  kind: Role
  name: {{ include "scale-manager.fullname" . }}
  apiGroup: rbac.authorization.k8s.io
