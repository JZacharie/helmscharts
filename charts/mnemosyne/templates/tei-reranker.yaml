{{- if .Values.tei.reranker.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mnemosyne.fullname" . }}-tei-reranker
  labels:
    {{- include "mnemosyne.labels" . | nindent 4 }}
    app: tei-reranker
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "mnemosyne.selectorLabels" . | nindent 6 }}
      app: tei-reranker
  template:
    metadata:
      labels:
        {{- include "mnemosyne.selectorLabels" . | nindent 8 }}
        app: tei-reranker
    spec:
      {{- if .Values.tei.reranker.runtimeClassName }}
      runtimeClassName: {{ .Values.tei.reranker.runtimeClassName }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: tei
        image: "{{ .Values.tei.reranker.image.repository | default "ghcr.io/huggingface/text-embeddings-inference" }}:{{ .Values.tei.reranker.image.tag | default "cpu-latest" }}"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
        args:
        - --model-id
        - {{ .Values.tei.reranker.model }}
        - --port
        - "80"
        {{- if .Values.tei.reranker.dtype }}
        - --dtype
        - {{ .Values.tei.reranker.dtype }}
        {{- end }}
        {{- if .Values.tei.reranker.maxBatchTokens }}
        - --max-batch-tokens
        - {{ .Values.tei.reranker.maxBatchTokens | quote }}
        {{- end }}
        ports:
        - containerPort: 80
        env:
        - name: HUGGING_FACE_HUB_TOKEN
          valueFrom:
            secretKeyRef:
              name: {{ include "mnemosyne.fullname" . }}-secrets
              key: HF_TOKEN
        resources:
          requests:
            cpu: 2
            memory: 4Gi
          limits:
            cpu: 4
            memory: 8Gi
        volumeMounts:
        - name: data
          mountPath: /data
      nodeSelector:
        kubernetes.io/hostname: vm169
      tolerations:
      - key: special-node
        operator: Equal
        value: gpu
        effect: NoSchedule
      volumes:
      - name: data
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mnemosyne.fullname" . }}-tei-reranker
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    {{- include "mnemosyne.selectorLabels" . | nindent 4 }}
    app: tei-reranker
{{- end }}
