{{- if .Values.cronjob.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "mnemosyne.fullname" . }}
  labels:
    {{- include "mnemosyne.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.cronjob.schedule | quote }}
  concurrencyPolicy: {{ .Values.cronjob.concurrencyPolicy }}
  failedJobsHistoryLimit: {{ .Values.cronjob.failedJobsHistoryLimit }}
  successfulJobsHistoryLimit: {{ .Values.cronjob.successfulJobsHistoryLimit }}
  jobTemplate:
    spec:
      template:
        metadata:
          {{- with .Values.podAnnotations }}
          annotations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          labels:
            {{- include "mnemosyne.selectorLabels" . | nindent 12 }}
        spec:
          {{- with .Values.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          serviceAccountName: {{ include "mnemosyne.serviceAccountName" . }}
          securityContext:
            {{- toYaml .Values.podSecurityContext | nindent 12 }}
          containers:
            - name: {{ .Chart.Name }}
              securityContext:
                {{- toYaml .Values.securityContext | nindent 16 }}
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              env:
                - name: DATABASE_URL
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "mnemosyne.fullname" . }}-secrets
                      key: DATABASE_URL
                - name: LITELLM_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "mnemosyne.fullname" . }}-secrets
                      key: LITELLM_API_KEY
                - name: COLLECTION_NAME
                  value: {{ .Values.config.collectionName | quote }}
                - name: EMBEDDING_MODEL
                  value: {{ .Values.config.embeddingModel | quote }}
                - name: LITELLM_URL
                  value: {{ .Values.config.litellmUrl | quote }}
                - name: NFS_PATH
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "mnemosyne.fullname" . }}-secrets
                      key: NFS_PATH
                - name: QDRANT_URL
                  value: {{ .Values.qdrant.url | quote }}
                - name: TEI_EMBEDDER_URL
                  value: {{ .Values.tei.embedder.url | quote }}
                - name: TEI_RERANKER_URL
                  value: {{ .Values.tei.reranker.url | quote }}
              volumeMounts:
                {{- range .Values.persistence.pvcs }}
                - name: {{ .name }}
                  mountPath: {{ .mountPath }}
                {{- end }}
                {{- range .Values.persistence.volumes }}
                - name: {{ .name }}
                  mountPath: {{ .mountPath }}
                {{- end }}
              resources:
                {{- toYaml .Values.resources | nindent 16 }}
          restartPolicy: OnFailure
          volumes:
            {{- range .Values.persistence.pvcs }}
            - name: {{ .name }}
              persistentVolumeClaim:
                claimName: {{ .claimName }}
            {{- end }}
            {{- range .Values.persistence.volumes }}
            - name: {{ .name }}
              {{- omit . "name" "mountPath" "readOnly" | toYaml | nindent 14 }}
            {{- end }}
          {{- with .Values.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.affinity }}
          affinity:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
{{- end }}
